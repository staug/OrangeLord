# TEST
import pygame, sys, time, random
from pygame.locals import *
import dMapModule
import Object, Tile, Utilities


gameSurface = pygame.display.set_mode((Utilities.NBSPRITX_DISP*Utilities.SPRITEWIDTH, Utilities.NBSPRITY_DISP*Utilities.SPRITEHEIGHT), 0, 32)
pygame.display.set_caption('Sprites and Sound')
entireWindowSurface = pygame.Surface((Utilities.NBSPRITX*Utilities.SPRITEWIDTH, Utilities.NBSPRITY*Utilities.SPRITEHEIGHT))


worldmap = []
possibleBeginPlace=[]
somename= dMapModule.dMap()
somename.makeMap(NBSPRITX,NBSPRITY,30,0,80) 
for y in range(Utilities.NBSPRITY):
        for x in range(Utilities.NBSPRITX):
                if somename.mapArr[y][x]==0:
                        possibleBeginPlace.append((x,y))
#                if somename.mapArr[y][x]==1:
#                        line += " "
                if somename.mapArr[y][x]==2:
                        worldmap.append(Tile(x, y, True))
#                if somename.mapArr[y][x]==3 or somename.mapArr[y][x]==4 or somename.mapArr[y][x]==5:


Player = 

# run the game loop
while True:
    # check for the QUIT event
    for event in pygame.event.get():
        if event.type == QUIT:
            pygame.quit()
            sys.exit()
